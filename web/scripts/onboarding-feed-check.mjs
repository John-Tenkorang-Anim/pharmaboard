import {chromium} from 'playwright';
import assert from 'node:assert/strict';
const browser=await chromium.launch();const page=await browser.newPage({viewport:{width:1440,height:1000}});const errors=[];page.on('pageerror',e=>errors.push(e.message));await page.goto('http://127.0.0.1:5174/login');assert.equal(await page.getByLabel('Full name',{exact:true}).count(),0);await page.getByRole('button',{name:'Create account',exact:true}).click();await page.getByLabel('Full name',{exact:true}).fill('Alex Bennett');await page.getByLabel('Role',{exact:true}).selectOption('student');await page.getByLabel('Field of study or work').selectOption('Computer Engineering');await page.getByLabel('School, university or organisation').fill('Example Institute of Technology');const phone='+233'+String(Date.now()).slice(-9);await page.getByLabel('Phone number',{exact:true}).fill(phone);await page.getByRole('button',{name:'Continue',exact:true}).click();await page.getByRole('button',{name:'Verify and continue',exact:true}).click();await page.getByRole('heading',{name:'Good to see you, Alex.'}).waitFor();await page.getByRole('link',{name:'Your profile',exact:true}).click();await page.getByText('Example Institute of Technology',{exact:true}).waitFor();await page.getByText('Computer Engineering',{exact:true}).waitFor();await page.getByRole('button',{name:'Sign out',exact:true}).click();await page.getByLabel('Phone number',{exact:true}).fill(phone);await page.getByRole('button',{name:'Continue',exact:true}).click();await page.getByRole('button',{name:'Verify and continue',exact:true}).click();await page.getByRole('link',{name:'Your profile',exact:true}).waitFor();await page.goto('http://127.0.0.1:5174/home');await page.getByRole('heading',{name:'Good to see you, Alex.'}).waitFor();
await page.getByRole('button',{name:'Latest discussions',exact:true}).waitFor();await page.locator('article').first().waitFor();assert.equal(await page.getByRole('region',{name:'Post replies'}).count(),0);const card=page.locator('article').first();await card.getByRole('button',{name:/^\d+ repl(y|ies)$/}).click();await card.getByLabel('Write a reply').waitFor();await card.getByRole('button',{name:/^\d+ repl(y|ies)$/}).click();assert.equal(await card.getByLabel('Write a reply').count(),0);await page.getByRole('button',{name:'Following',exact:true}).click();await page.getByRole('button',{name:'Latest discussions',exact:true}).click();await page.screenshot({path:'/tmp/pharmaboard-workspace-qa/general-feed.png'});await page.getByRole('button',{name:'Sign out',exact:true}).click();await page.getByRole('button',{name:'Create account',exact:true}).click();await page.setViewportSize({width:390,height:844});await page.screenshot({path:'/tmp/pharmaboard-workspace-qa/onboarding-mobile.png',fullPage:true});assert(await page.evaluate(()=>document.documentElement.scrollWidth<=innerWidth));assert.deepEqual(errors,[]);await browser.close();console.log('PASS multidisciplinary signup persistence, separate sign-in, school profile, full overview feed, click-only replies, mobile onboarding');
