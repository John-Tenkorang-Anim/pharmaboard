import {chromium} from 'playwright';
import assert from 'node:assert/strict';
const browser=await chromium.launch();const page=await browser.newPage({viewport:{width:1440,height:1000}});const errors=[];page.on('pageerror',e=>errors.push(e.message));await page.goto('http://127.0.0.1:5174/network');await page.getByRole('button',{name:'Explore sample workspace'}).click();await page.getByRole('heading',{name:'My network',exact:true}).waitFor();await page.getByLabel('Search network').fill('Ama Boateng · Preview');await page.waitForTimeout(600);assert.equal(await page.locator('article').count(),1);await page.getByRole('button',{name:'Quick view',exact:true}).click();const dialog=page.getByRole('dialog');await dialog.getByRole('heading',{name:'Ama Boateng · Preview',exact:true}).waitFor();const following=await dialog.getByRole('button',{name:'Following',exact:true}).count();await dialog.getByRole('button',{name:following?'Following':'Follow',exact:true}).click();await dialog.getByRole('button',{name:following?'Follow':'Following',exact:true}).waitFor();await page.keyboard.press('Escape');await page.getByRole('button',{name:'Quick view',exact:true}).click();await dialog.getByRole('button',{name:following?'Follow':'Following',exact:true}).waitFor();await dialog.getByRole('button',{name:following?'Follow':'Following',exact:true}).click();await dialog.getByRole('button',{name:following?'Following':'Follow',exact:true}).waitFor();await page.keyboard.press('Escape');await page.getByLabel('Search network').fill('Preview');await page.waitForTimeout(600);await page.screenshot({path:'/tmp/pharmaboard-workspace-qa/network-desktop.png'});await page.setViewportSize({width:390,height:844});await page.screenshot({path:'/tmp/pharmaboard-workspace-qa/network-mobile.png',fullPage:true});assert(await page.evaluate(()=>document.documentElement.scrollWidth<=innerWidth));assert.deepEqual(errors,[]);await browser.close();console.log('PASS profile search, quick view, persisted follow/unfollow, mobile layout, no browser errors');
