// Package identity owns accounts, authentication, sessions, devices, and
// professional verification. Other modules access identity data only through
// exported interfaces; they must not query identity tables directly.
package identity
